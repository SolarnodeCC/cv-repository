# Pin RenderCV for reproducible local + CI builds.
# Bump deliberately; update the schema URL in cv.yaml to match.
rendercv[full]==2.8

# Local CV web editor (make web)
fastapi>=0.115,<0.120
uvicorn[standard]>=0.32,<0.40
pyyaml>=6,<7
